"const fs = require('fs');\nconst path = require('path');\n\nconst filePath = path.join(__dirname, 'POSBilling.tsx');\nlet content = fs.readFileSync(filePath, 'utf8');\n\nconst startMarker = '  return (';\nconst startIdx = content.indexOf(startMarker);\nif (startIdx === -1) {\n  console.error('Could not find start marker!');\n  process.exit(1);\n}\n\nconst prefixContent = content.slice(0, startIdx);\n\nconst newReturnBlock = `  return (\n    <div className={\\`space-y-6 font-sans text-black text-left select-none antialiased pt-6 transition-all duration-300 \\${scanFlash ? 'bg-emerald-50' : ''}\\`}>\n      {/* WORKFLOW CONDITIONAL STEP 1: BILLING TERMINAL SCREEN */}\n      {currentStep === 'BILLING' && (\n        <>\n          {/* Page Title Header */}\n          <div className=\"flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 py-4 border-b border-slate-200\">\n            <h1 className=\"text-[24px] font-semibold text-[#000000] tracking-tight\">\n              POS Billing\n            </h1>\n            <div className=\"flex items-center gap-4 text-xs font-semibold text-slate-500\">\n              <div>\n                <span className=\"text-[9px] uppercase font-bold text-slate-400 block\">Bill Number</span>\n                <span className=\"text-sm font-semibold text-black\">{activeBillNumber}</span>\n              </div>\n              <div className=\"h-8 w-px bg-slate-200\" />\n              <div>\n                <span className=\"text-[9px] uppercase font-bold text-slate-400 block\">Current Date</span>\n                <span className=\"text-sm font-semibold text-black\">{currentDate}</span>\n              </div>\n            </div>\n          </div>\n\n          <div className=\"grid grid-cols-1 lg:grid-cols-12 gap-6 items-start\">\n            {/* LEFT AREA: PRODUCT CATALOG & CATEGORIES GRID */}\n            <div className=\"lg:col-span-8 space-y-6 text-left\">\n              {/* Hidden input to receive physical scanner machine codes */}\n              <input\n                ref={sca
<truncated 56207 bytes>